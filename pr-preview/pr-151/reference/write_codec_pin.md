# Write a CoDEC data table to the local CoDEC board directory

This function is used by developers creating and updating CoDEC data
tables.

## Usage

``` r
codec_board_local_dev()

write_codec_pin(x)
```

## Arguments

- x:

  a codec_tbl object created with as_codec_tbl()
